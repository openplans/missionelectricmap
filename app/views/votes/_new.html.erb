<%= form_for [supportable, supportable.votes.new], :url => feature_point_votes_url(supportable), :html => { :class => "vote", "data-behavior" => "load_result_in_popup" } do |form| %>
  <button type="submit">
    <div>
      <%= t 'feature.vote_action' %>
    </div>
  </button>
<% end %>